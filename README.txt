The Prayer Overhead Text plugin shows text above the players head when a certain prayer threshold is met
This plugin has the current config options
    * Low prayer Warning Text - This is the text that appears above the players head when the threshold is met
    * Prayer Threshold - This is the value that dictates what prayer level is required to trigger the text
    * Text Colour -  This sets the colour of the displayed warning text